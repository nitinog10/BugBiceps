# nitinog10/BugBiceps — Documentation

> Auto-generated by [DocuVerse](https://docuverse.ai)

## Overview

The `repo_f5bb5f002c6c` repository hosts the source code for the BugBiceps web application, a comprehensive platform designed to showcase various services, case studies, and technologies. The primary purpose of this application is to provide users with an engaging and informative interface that highlights the capabilities and achievements of BugBiceps. The tech stack includes React for the frontend, Vite as the build tool, and Netlify for deployment. The application is structured to leverage modern web development practices, ensuring a seamless user experience across different devices.

The high-level architecture of the application is divided into several key components and sections. The `src` directory contains the core React components and sections that make up the application. Notable components include `CustomCursor.jsx`, `MagneticButton.jsx`, and `Navbar.jsx`, which provide interactive and navigational elements. The `sections` directory houses various React components such as `Hero.jsx`, `Services.jsx`, and `Testimonials.jsx`, each responsible for rendering specific sections of the webpage.

The intended audience for this repository includes web developers, designers, and stakeholders interested in understanding the implementation and structure of the BugBiceps web application. The repository is designed to be easily navigable, with clear documentation in the `README.md` file that outlines the purpose, dependencies, and setup instructions. Additionally, the `netlify.toml` file configures the deployment settings for Netlify, ensuring that the application can be quickly and efficiently deployed to a live environment.

## Architecture

```markdown
- **Layers / Modules**:
  - **Public Assets**: Static files and configurations (`public/`).
  - **Configuration**: Project configurations (`netlify.toml`, `vite.config.js`).
  - **Source Code**:
    - **Components**: Reusable UI components (`src/components/`).
    - **Sections**: Page sections (`src/sections/`).
    - **App Structure**: Main application structure (`src/App.jsx`, `src/main.jsx`).
    - **Styles**: Global styles (`src/index.css`).

- **Data Flow**:
  - The application starts from `src/main.jsx`, which renders the `App` component defined in `src/App.jsx`.
  - `App.jsx` imports and utilizes various components and sections to build the UI.
  - Components like `Navbar`, `CustomCursor`, and `ScrollProgress` are used across different sections for consistent UI behavior.
  - Sections such as `Hero`, `Services`, and `Contact` are rendered within `App.jsx` to form the complete page layout.

- **Key Design Patterns**:
  - **Component-Based Architecture**: Utilizes reusable React components for different UI elements.
  - **Sectional Division**: Divides the application into distinct sections for better maintainability and readability.
  - **Global State Management**: Although not explicitly mentioned, the structure allows for easy integration of state management solutions if needed.

- **Entry Points**:
  - **`src/main.jsx`**: The main entry point of the React application.
  - **`index.html`**: The HTML file that serves as the root of the web application.
  - **`vite.config.js`**: Configuration file for the Vite build tool, handling module bundling and development server setup.
  - **`netlify.toml`**: Configuration file for Netlify, managing deployment settings and headers.
```

## Folder Structure

```
├── README.md
├── Untitled design (1).png
├── index.html
├── netlify.toml
├── package.json
├── vite.config.js
├── public/
│   ├── _headers
│   └── logo.png
└── src/
    ├── App.jsx
    ├── index.css
    ├── main.jsx
    ├── components/
    │   ├── CustomCursor.jsx
    │   ├── MagneticButton.jsx
    │   ├── Navbar.jsx
    │   ├── Preloader.jsx
    │   └── ScrollProgress.jsx
    └── sections/
        ├── AIShowcase.jsx
        ├── CaseStudies.jsx
        ├── Contact.jsx
        ├── Education.jsx
        ├── Footer.jsx
        ├── Hero.jsx
        ├── Philosophy.jsx
        ├── Services.jsx
        ├── Stats.jsx
        ├── Technologies.jsx
        └── Testimonials.jsx
```

## Dependencies

## Dependencies

### Production Dependencies

- **gsap** (`^3.12.5`): A JavaScript library for high-performance animations that makes it easy to create complex animations.
- **lenis** (`^1.1.18`): A smooth scroll library for creating smooth scrolling animations.
- **react** (`^18.3.1`): A JavaScript library for building user interfaces, particularly single-page applications.
- **react-dom** (`^18.3.1`): The DOM library for React, which provides DOM-specific methods that can be used at the top level of a web app to enable an efficient user interface.
- **react-icons** (`^5.5.0`): A library of popular icon packs that can be used with React.

### Development Dependencies

- **@vitejs/plugin-react** (`^4.3.4`): A Vite plugin that provides React support, enabling fast development with React.
- **vite** (`^6.0.5`): A build tool that aims to provide a faster and leaner development experience for modern web projects.

## File Documentation

### README.md

#### Overview

# BugBiceps

This README file provides an overview of the BugBiceps web application, detailing its purpose, dependencies, and key components. It serves as a guide for developers to understand the project structure, installation process, and development workflow.

#### Dependencies

| Dependency | Version | Purpose |
|------------|---------|---------|
| React      | 18.3.1  | UI library |
| Vite       | 6.0.5   | Build tool and dev server |
| GSAP       | 3.12.5  | Animation library with ScrollTrigger |
| Lenis      | 1.1.18  | Smooth scrolling library |

#### Classes

| Class | Purpose | Key Methods |
|-------|---------|-------------|
| CustomCursor | Enhanced cursor with interactive effects | `updateCursor()`, `setCursorStyle()` |
| MagneticButton | Buttons with magnetic hover effects | `applyMagneticEffect()`, `resetMagneticEffect()` |
| Navbar | Sticky navigation bar | `toggleMenu()`, `scrollToSection()` |

#### Functions

| Function | Parameters | Returns | Description |
|----------|------------|---------|-------------|
| updateCursor | cursorStyle | void | Updates the cursor style |
| applyMagneticEffect | element, hoverState | void | Applies magnetic effect to an element |
| toggleMenu | menuState | void | Toggles the visibility of the navigation menu |
| scrollToSection | sectionId | void | Scrolls to a specific section of the page |

#### Configuration

- **Development Server Port**: 5173
- **Production Build Preview**: Available via `npm run preview`

#### Notes / Edge Cases

- Ensure Node.js version 16 or higher is installed before running the project.
- Regression tests should be run before any major changes to high-risk components.
- The smooth scrolling feature may not work correctly on older browsers.

### index.html

#### Overview

# index.html Documentation

#### Module Overview

The `index.html` file serves as the primary entry point for the BugBiceps web application. It sets up the basic HTML structure, includes meta tags for SEO and social media sharing, and links to external stylesheets and JavaScript modules required for rendering and functionality.

#### Dependencies

- **External Stylesheets:**
  - [Google Fonts](https://fonts.googleapis.com/css2?family=Sora:wght@300;400;500;600;700;800&family=Inter:wght@300;400;500;600;700;800&family=JetBrains+Mono:wght@400;500;600&display=swap)
- **External JavaScript:**
  - `/src/main.jsx`

#### Classes

| Class | Purpose | Key Methods |
| --- | --- | --- |
| N/A | N/A | N/A |

#### Functions

| Function | Parameters | Returns | Description |
| --- | --- | --- | --- |
| N/A | N/A | N/A | N/A |

#### Configuration

- **Meta Tags:**
  - `charset`: UTF-8
  - `viewport`: width=device-width, initial-scale=1.0
  - `description`: BugBiceps — Engineering Digital Systems That Scale. Premium web, mobile, AI systems, automation, and design solutions.
  - `keywords`: BugBiceps, digital engineering, AI systems, web development, automation, SEO, 3D visualization
  - `author`: BugBiceps
  - `og:title`: BugBiceps | Engineering Digital Systems That Scale
  - `og:description`: Premium digital engineering — Web, Mobile, AI, Automation, SEO, and 3D solutions built with precision.
  - `og:type`: website

#### Notes / Edge Cases

- The `<div id="root"></div>` element is the mounting point for the React application.
- The `src` attribute in `<script type="module" src="/src/main.jsx"></script>` should be updated to the correct path if the project structure changes.
- Ensure that the linked Google Fonts are accessible and correctly loaded to avoid rendering issues.

### netlify.toml

#### Overview

# Netlify Configuration (`netlify.toml`)

#### Module Overview

The `netlify.toml` file is used to configure the build and deployment settings for projects hosted on Netlify. This file specifies the build command and the directory where the build artifacts should be published, enabling seamless integration with Netlify's continuous deployment pipeline.

#### Dependencies

- **None**

#### Configuration

### Configuration

| Key         | Value           | Purpose                                                                 |
|-------------|------------------|-------------------------------------------------------------------------|
| `command`   | `npm run build` | Specifies the command to run for building the project.                |
| `publish`   | `dist`           | Specifies the directory where the build artifacts should be published. |

#### Notes / Edge Cases

- Ensure that the `npm run build` command is correctly defined in your `package.json` file.
- The `dist` directory should be the default output directory for your build process.
- If using a different build system, update the `command` accordingly.
- Verify that Netlify's build settings match your local development environment to avoid discrepancies during deployment.

### package.json

#### Overview

# `package.json` Documentation

#### Module Overview

The `package.json` file is a core component of Node.js projects, defining the project metadata, dependencies, and scripts. It serves as a manifest that contains all necessary information about the project, including its dependencies, scripts for automation, and versioning.

#### Dependencies

| Dependency | Version | Purpose |
|------------|---------|---------|
| gsap       | ^3.12.5 | Animation library for JavaScript. |
| lenis      | ^1.1.18 | Smooth scrolling library. |
| react      | ^18.3.1 | Popular JavaScript library for building user interfaces. |
| react-dom  | ^18.3.1 | Library for rendering React to the DOM. |
| react-icons | ^5.5.0  | Collection of popular icon sets for React. |

#### Scripts

| Script | Command | Description |
|--------|---------|-------------|
| dev    | vite    | Starts the development server. |
| build  | vite build | Builds the project for production. |
| preview| vite preview | Previews the production build. |

#### Notes / Edge Cases

- The `private` field is set to `true`, indicating that this package is intended for internal use and should not be published to a public registry.
- The `type` field is set to `module`, indicating that the project uses ES modules.
- No classes or functions are defined directly in this file, but they are imported from the dependencies listed above.

### vite.config.js

#### Overview

# `vite.config.js` Documentation

#### Module Overview

The `vite.config.js` file is the primary configuration file for a Vite-based project. It defines the configuration settings and plugins required to build and serve the application. This file ensures that the development server runs on the specified port and includes the React plugin for JSX support.

#### Dependencies

- `vite`: A modern front-end build tool that aims to provide a faster and leaner development experience.
- `@vitejs/plugin-react`: A plugin to add React support to Vite.

#### Configuration

| Class | Purpose | Key Methods |
| --- | --- | --- |
| None | N/A | N/A |

#### Functions

| Function | Parameters | Returns | Description |
| --- | --- | --- | --- |
| `defineConfig` | `config` | `Config` | A function to define the Vite configuration. |

#### Notes / Edge Cases

- Ensure that the `@vitejs/plugin-react` is installed in the project dependencies.
- The `open` property in the `server` configuration determines whether the browser should automatically open when the server starts. Setting it to `false` means the browser will not open automatically.
- The `port` can be changed to any available port number as per project requirements.

### public/_headers

#### Overview

# public/_headers Module Overview

The `public/_headers` file defines HTTP header configurations for various file types within the public directory. This file ensures that appropriate content types and caching policies are applied to enhance the performance and security of served assets.

#### Dependencies

No direct dependencies are listed in this file.

#### Configuration

| Configuration | Purpose |
|--------------|---------|
| `Content-Type: application/javascript; charset=utf-8` | Sets the content type for JavaScript files to `application/javascript` with UTF-8 charset. |
| `Content-Type: text/css; charset=utf-8` | Sets the content type for CSS files to `text/css` with UTF-8 charset. |
| `Cache-Control: public, max-age=0, must-revalidate` | Configures caching policy to ensure that the resource is not cached and must be revalidated on every request. |

#### Notes / Edge Cases

- This file does not contain any classes or functions.
- The `Cache-Control` directive is set to `must-revalidate` which means that the client must always validate the resource with the server before using a cached copy.
- Ensure that the server correctly parses and applies these headers to the respective file types.

### src/App.jsx

#### Overview

# src/App.jsx

#### Module Overview

The `src/App.jsx` file serves as the main entry point for the application. It initializes the React application, manages the loading state, and integrates various components and libraries to create a smooth, interactive user experience. This file is crucial for setting up the application's structure and ensuring that all components are rendered correctly.

#### Dependencies

| Dependency | Description |
|------------|-------------|
| `react` | Core React library for building user interfaces. |
| `useState`, `useEffect`, `useCallback` | React hooks for state management, lifecycle methods, and memoization. |
| `Lenis` | A smooth scroll library for creating physics-based scrolling. |
| `gsap` | GreenSock Animation Platform for creating high-performance animations. |
| `ScrollTrigger` | GSAP plugin for triggering animations based on scroll position. |
| `./components/Preloader` | Custom component for displaying a preloader during page load. |
| `./components/CustomCursor` | Custom component for displaying a custom cursor. |
| `./components/Navbar` | Custom component for the navigation bar. |
| `./components/ScrollProgress` | Custom component for displaying scroll progress. |
| `./sections/Hero` | Section component for the hero section. |
| `./sections/Services` | Section component for the services section. |
| `./sections/AIShowcase` | Section component for the AI showcase section. |
| `./sections/CaseStudies` | Section component for the case studies section. |
| `./sections/Philosophy` | Section component for the philosophy section. |
| `./sections/Education` | Section component for the education section. |
| `./sections/Testimonials` | Section component for the testimonials section. |
| `./sections/Stats` | Section component for the stats section. |
| `./sections/Contact` | Section component for the contact section. |
| `./sections/Footer` | Section component for the footer. |

#### Functions

| Function | Parameters | Returns | Description |
|----------|------------|---------|-------------|
| `App` | None | JSX | The main function that renders the application. It manages the loading state and initializes smooth scrolling and animations. |

#### Notes / Edge Cases

- Ensure that all imported components are correctly implemented and exported from their respective files.
- The smooth scrolling and animations should be tested across different browsers and devices to ensure compatibility.
- The `loading` state should be managed carefully to avoid rendering issues or performance bottlenecks.
- The global `lenis` instance should be properly cleaned up to prevent memory leaks.

### src/index.css

#### Overview

# src/index.css Documentation

#### Module Overview

The `src/index.css` file serves as the primary stylesheet for the BugBiceps Design System v2.0. It establishes a comprehensive set of CSS variables, utility classes, and animations to ensure a consistent and visually appealing design across the application. This file is crucial for maintaining design integrity and providing a unified look and feel.

#### Dependencies

- None (This file does not import any external dependencies)

#### Classes

| Class                | Purpose                                                                                       |
|----------------------|-----------------------------------------------------------------------------------------------|
| `.glass`             | Applies a glass effect with backdrop blur and border.                                         |
| `.gradient-text`     | Applies a gold gradient to text with background clip.                                         |
| `.gradient-accent-text` | Applies an accent gradient to text with background clip.                                   |
| `.section-container` | Centers content with maximum width and padding.                                              |
| `.noise-overlay`     | Adds a fixed noise overlay with a fractal noise pattern.                                     |
| `.section-divider`   | Creates a horizontal divider with a subtle gradient effect.                                  |

#### Configuration

| Variable                 | Value                                                                                       | Description                                                                                       |
|--------------------------|---------------------------------------------------------------------------------------------|---------------------------------------------------------------------------------------------------|
| `--gold`                 | `#F0B020`                                                                                   | Primary gold color derived from the BugBiceps logo.                                               |
| `--gold-light`           | `#FFD54F`                                                                                   | Lighter shade of gold.                                                                            |
| `--gold-dark`            | `#C48A10`                                                                                   | Darker shade of gold.                                                                             |
| `--gold-dim`             | `rgba(240, 176, 32, 0.6)`                                                                   | Semi-transparent gold color.                                                                      |
| `--red`                  | `#E83030`                                                                                   | Primary red color derived from the BugBiceps logo.                                                 |
| `--red-light`            | `#FF5252`                                                                                   | Lighter shade of red.                                                                             |
| `--red-dim`              | `rgba(232, 48, 48, 0.6)`                                                                    | Semi-transparent red color.                                                                       |
| `--orange`               | `#F06020`                                                                                   | Primary orange color.                                                                             |
| `--orange-light`         | `#FF8040`                                                                                   | Lighter shade of orange.                                                                          |
| `--orange-dim`           | `rgba(240, 96, 32, 0.6)`                                                                    | Semi-transparent orange color.                                                                   |
| `--bg-primary`           | `#060608`                                                                                   | Primary background color.                                                                         |
| `--bg-secondary`         | `#0A0A10`                                                                                   | Secondary background color.                                                                       |
| `--bg-surface`           | `#101018`                                                                                   | Surface background color.                                                                         |
| `--bg-surface-light`     | `#18182A`                                                                                   | Lighter surface background color.                                                                 |
| `--bg-card`              | `rgba(16, 16, 24, 0.85)`                                                                    | Card background color with opacity.                                                               |
| `--text-primary`         | `#F0F0F8`                                                                                   | Primary text color.                                                                               |
| `--text-secondary`       | `#8888A0`                                                                                   | Secondary text color.                                                                             |
| `--text-muted`           | `#555570`                                                                                   | Muted text color.                                                                                 |
| `--gradient-gold`        | `linear-gradient(135deg, #F0B020 0%, #F06020 100%)`                                          | Gold gradient.                                                                                    |
| `--gradient-red-gold`    | `linear-gradient(135deg, #E83030 0%, #F0B020 100%)`                                          | Red to gold gradient.                                                                             |
| `--gradient-glow`        | `linear-gradient(135deg, rgba(240, 176, 32, 0.15) 0%, rgba(240, 96, 32, 0.08) 100%)`          | Glow gradient.                                                                                    |
| `--gradient-accent`      | `linear-gradient(135deg, #F0B020 0%, #F06020 50%, #E83030 100%)`                             | Accent gradient.                                                                                  |
| `--glow-gold`            | `0 0 40px rgba(240, 176, 32, 0.3)`                                                           | Gold glow effect.                                                                                 |
| `--glow-gold-strong`     | `0 0 80px rgba(240, 176, 32, 0.2), 0 0 160px rgba(240, 176, 32, 0.08)`                       | Stronger gold glow effect.                                                                        |
| `--glow-red`             | `0 0 40px rgba(232, 48, 48, 0.3)`                                                           | Red glow effect.                                                                                  |
| `--glow-orange`          | `0 0 40px rgba(240, 96, 32, 0.3)`                                                            | Orange glow effect.                                                                               |
| `--font-heading`         | `'Sora', -apple-system, sans-serif`                                                          | Font family for headings.                                                                         |
| `--font-body`            | `'Inter', -apple-system, sans-serif`                                                         | Font family for body text.                                                                        |
| `--font-mono`            | `'JetBrains Mono', monospace`                                                                | Font family for monospaced text.                                                                  |
| `--section-pad`          | `clamp(100px, 12vw, 200px)`                                                                  | Padding for sections.                                                                             |
| `--container-max`        | `1400px`                                                                                     | Maximum width for containers.                                                                     |
| `--ease-out-expo`        | `cubic-bezier(0.16, 1, 0.3, 1)`                                                              | Easing function for exponential ease-out.                                                         |
| `--ease-out-quart`       | `cubic-bezier(0.25, 1, 0.5, 1)`                                                              | Easing

### src/main.jsx

#### Overview

# `src/main.jsx` Documentation

#### Module Overview

The `src/main.jsx` file is the entry point for the React application. It initializes the React root component and renders the `App` component within a strict mode to ensure that all components in the tree are rendered using the strict mode behavior. This helps identify potential issues in the application.

#### Dependencies

- `react`
- `react-dom/client`

#### Functions

| Function | Parameters | Returns | Description |
| --- | --- | --- | --- |
| `ReactDOM.createRoot(document.getElementById('root')).render` | `React.StrictMode`, `<App />` | None | Renders the `App` component within a strict mode container. |

#### Notes / Edge Cases

- Ensure that the DOM element with the id `root` exists before calling `ReactDOM.createRoot`.
- Strict mode helps in identifying potential problems in the application but may cause some components to re-render more often. Monitor performance impacts during development.

### src/components/CustomCursor.jsx

#### Overview

# CustomCursor.jsx

#### Module Overview

The `CustomCursor.jsx` file defines a custom cursor component for the application, providing an enhanced user experience by creating a visually appealing and interactive cursor. This component is designed to follow the mouse movements with a smooth animation effect, making the cursor more engaging and dynamic.

#### Dependencies

| Dependency | Description |
|------------|-------------|
| `useEffect` | React hook to handle side effects in functional components. |
| `useRef` | React hook to persist values across renders without causing re-renders. |
| `useMousePosition` | Custom hook to track the mouse position. |
| `useInteractiveListeners` | Custom hook to manage interactive event listeners. |

#### Functions

| Function | Parameters | Returns | Description |
|----------|------------|---------|-------------|
| `CustomCursor` | None | JSX Element | Renders the custom cursor component. |

#### Notes / Edge Cases

- The custom cursor is only rendered for screen widths greater than 768 pixels to avoid unnecessary rendering on smaller screens.
- The animation and transitions are designed to provide a smooth and visually appealing cursor movement experience.
- Ensure that the `useMousePosition` and `useInteractiveListeners` hooks are properly implemented and tested to avoid any runtime errors.

### src/components/MagneticButton.jsx

#### Overview

# MagneticButton.jsx Documentation

#### Module Overview

The `MagneticButton` component is a custom button that applies a magnetic effect on mouse movement. This effect visually pulls the button towards the cursor, creating an interactive and engaging user experience.

#### Dependencies

- `react`: React library for building user interfaces.

#### Classes

| Class | Purpose | Key Methods |
| --- | --- | --- |
| None | N/A | N/A |

#### Functions

| Function | Parameters | Returns | Description |
| --- | --- | --- | --- |
| `MagneticButton` | `children`, `style`, `onClick`, `className`, `id` | JSX.Element | Renders a button with a magnetic effect on mouse movement. |

#### Configuration

- `children`: The content inside the button.
- `style`: Additional inline styles for the button.
- `onClick`: Function to be called when the button is clicked.
- `className`: Additional CSS classes for the button.
- `id`: Unique identifier for the button.

#### Notes / Edge Cases

- The button will not move if the cursor does not enter the button's area.
- The `handleMouseLeave` function resets the button's transform to its original position when the cursor leaves the button area.
- The transition effect may not be visible on very fast cursor movements due to the `0.4s` duration.

### src/components/Navbar.jsx

#### Overview

# Navbar Component Documentation

#### Module Overview

The `Navbar` component is a React functional component designed to render a navigation bar for the application. It includes a logo, navigation links, and a mobile menu toggle. The component is responsive and includes animations for visual appeal.

#### Dependencies

- `useEffect`, `useRef`, `useState` from `react`
- `gsap` from `gsap`
- `handleSmoothScroll` from `../utils/navigationUtils`

#### Functions

| Function | Parameters | Returns | Description |
|----------|------------|---------|-------------|
| `Navbar` | None | JSX Element | Renders the navigation bar component |

#### Configuration / Constants

- `links`: An array of objects defining the navigation links with `label` and `href` properties.

#### Notes / Edge Cases

- The mobile menu is hidden on screens wider than 768px.
- The navigation bar changes style when the user scrolls more than 60px from the top.
- GSAP animations are used for entrance and interaction effects.
- Smooth scrolling is applied to all navigation links using `handleSmoothScroll`.

### src/components/Preloader.jsx

#### Overview

# Preloader Component Documentation

#### Module Overview

The `Preloader.jsx` file defines a React functional component that serves as a preloader for the application. It displays an animated loading sequence with a logo, a tagline, and a progress bar. This component is used to enhance the user experience by providing visual feedback during the initial loading phase.

#### Dependencies

| Dependency | Description |
|------------|-------------|
| `useState` | React hook for managing state. |
| `useEffect` | React hook for performing side effects. |
| `useRef` | React hook for creating references to DOM elements. |
| `gsap` | GreenSock Animation Platform (GSAP) for creating animations. |

#### Functions

| Function | Parameters | Returns | Description |
|----------|------------|---------|-------------|
| `Preloader` | `{ onComplete }` | JSX | A functional component that renders a preloader with animations. |

#### Notes / Edge Cases

- Ensure that the `onComplete` callback is a function to avoid runtime errors.
- The `gsap` library must be installed and imported correctly for the animations to work.
- The `preloaderRef`, `logoRef`, `counterRef`, `barRef`, and `taglineRef` should be used to reference the respective DOM elements for animations.
- The `useEffect` hook is used to trigger the animation sequence when the component mounts. It cleans up the animation timeline when the component unmounts.
- The animation sequence includes a dark screen, logo entrance with a glow effect, tagline appearance, counter animation, progress bar animation, and an exit sequence.

### src/components/ScrollProgress.jsx

#### Overview

# ScrollProgress.jsx Documentation

#### Module Overview

The `ScrollProgress.jsx` component is designed to visually represent the scroll progress of the webpage. It provides a smooth, animated progress bar at the top of the viewport that fills up as the user scrolls down the page. This component enhances user experience by giving visual feedback on how much of the page has been explored.

#### Dependencies

| Import | Source | Purpose |
| --- | --- | --- |
| `useEffect`, `useState` | React | Hooks for managing side effects and state in functional components. |

#### Classes

There are no classes defined in this file.

#### Functions

| Function | Parameters | Returns | Description |
| --- | --- | --- | --- |
| `ScrollProgress` | None | None | Main functional component that tracks scroll progress and renders the progress bar. |

#### Configuration

There are no configurable options in this component.

#### Constants

There are no constants defined in this file.

#### Notes / Edge Cases

- The progress bar is fixed at the top of the viewport and will not scroll out of view.
- The progress calculation assumes a standard document structure where `document.documentElement.scrollHeight` represents the total height of the document.
- The progress bar's width transitions smoothly to provide a visually appealing effect.
- The `passive: true` option in `addEventListener` ensures that the scroll event listener does not block the main thread, improving performance.
- The progress bar is hidden when the user has not scrolled beyond the viewport height.

### src/sections/AIShowcase.jsx

#### Overview

# AIShowcase.jsx Documentation

#### Module Overview

The `AIShowcase.jsx` file is a React component designed to display an animated section showcasing the company's AI capabilities. It utilizes HTML5 Canvas for particle animations and GSAP for scroll-triggered reveal effects. The component is styled to be responsive and visually engaging, highlighting key AI services and technologies.

#### Dependencies

- `useEffect`, `useRef` from 'react'
- `gsap` from 'gsap'
- `ScrollTrigger` from 'gsap/ScrollTrigger'

#### Functions

| Function | Parameters | Returns | Description |
|----------|------------|---------|-------------|
| AIShowcase | None | JSX Element | Main component function that renders the AI Showcase section. |

#### Configuration / Constants

| Name | Type | Value | Description |
|------|------|-------|-------------|
| `capabilities` | Array | [{ label: string, value: string, detail: string }] | Array of objects representing the AI capabilities to be displayed. |

#### Notes / Edge Cases

- The canvas animation is optimized for performance by using `requestAnimationFrame` and scaling the canvas based on the device pixel ratio.
- The GSAP animations are scoped to the component using `gsap.context` to ensure cleanup on unmount.
- The component is designed to be responsive, with media queries adjusting the grid layout on smaller screens.

### src/sections/CaseStudies.jsx

#### Overview

# `src/sections/CaseStudies.jsx` Documentation

#### Module Overview

The `CaseStudies.jsx` file is a React component designed to showcase a series of case studies or project highlights. It utilizes GSAP for animations and ScrollTrigger for scroll-based interactions, creating an engaging and dynamic presentation of selected work.

#### Dependencies

- `react`: For building the UI components.
- `gsap`: A powerful animation library used for creating the entrance animations for the case study cards.
- `react-icons/fa` and `react-icons/si`: For importing various technology icons used to represent the tech stack of each project.

#### Functions

| Function       | Parameters | Returns | Description                                                                                           |
|----------------|------------|---------|-------------------------------------------------------------------------------------------------------|
| `getTechIcon`  | `tech`     | `React Component` | Maps technology names to their corresponding icon components.                                          |

#### Configuration / Constants

- `projects`: An array of objects, each representing a case study with properties like `title`, `category`, `desc`, `tech`, `metric`, and `link`.

#### Notes / Edge Cases

- The GSAP animations are tied to the scroll position using ScrollTrigger, ensuring that they only play when the relevant elements are in view.
- The `handleMouseEnter` and `handleMouseLeave` functions are used to create a subtle hover effect on the case study cards, enhancing user interaction.
- The `getTechIcon` function returns `null` if the technology name does not match any in the icon map, which could lead to missing icons for unspecified technologies.

### src/sections/Contact.jsx

#### Overview

# `src/sections/Contact.jsx` Documentation

#### Module Overview

The `Contact.jsx` file is a React component that renders the "Contact" section of the website. It includes a contact form and contact information, with animations and interactive elements to enhance user experience.

#### Dependencies

- `useEffect`, `useRef`, `useState` from 'react'
- `gsap` from 'gsap'
- `ScrollTrigger` from 'gsap/ScrollTrigger'
- `FaLinkedin`, `FaPhone`, `FaEnvelope` from'react-icons/fa'
- `MagneticButton` from `../components/MagneticButton`

#### Functions

| Function      | Parameters | Returns | Description |
| ------------- | ---------- | ------- | ----------- |
| `Contact`     | None       | JSX     | Main component function that renders the Contact section. |

#### Configuration / Constants

- `inputBaseStyle`: Object containing base styles for input fields.
- `labelStyle`: Object containing styles for labels.

#### Notes / Edge Cases

- The form submission is currently prevented (`e.preventDefault()`) and should be hooked up to actual submission logic.
- Ensure that GSAP animations are tested across different browsers for consistent performance.
- The `MagneticButton` component should be reviewed for any additional props or methods that might be required.

### src/sections/Education.jsx

#### Overview

# `src/sections/Education.jsx` Documentation

#### Module Overview

The `Education.jsx` file is a React component designed to render the "Education" section of the website. This section highlights the company's mission, vision, and key focus points related to education and career stability. It utilizes GSAP for animations and ScrollTrigger for scroll-based interactions to enhance user experience.

#### Dependencies

- `useEffect`, `useRef` from 'react'
- `gsap` from 'gsap'
- `ScrollTrigger` from 'gsap/ScrollTrigger'
- Various icons from 'react-icons/fa'

#### Functions

| Function | Parameters | Returns | Description |
|----------|------------|---------|-------------|
| `Education` | None | JSX Element | Renders the Education section component |

#### Constants

| Constant | Type | Value | Description |
|----------|------|-------|-------------|
| `focusPoints` | Array of Objects | [{ icon, title, desc },...] | Array containing focus points with their respective icons, titles, and descriptions |

#### Notes / Edge Cases

- The component uses GSAP animations that are triggered on scroll. Ensure that the GSAP library and ScrollTrigger plugin are correctly loaded.
- The layout adapts to screen size, switching to a single-column layout on smaller screens (max-width: 600px).

### src/sections/Footer.jsx

#### Overview

# Footer.jsx Documentation

#### Module Overview

The `Footer.jsx` file defines the footer component for the website. This component is responsible for rendering the footer section, which includes the brand logo, a brief description, navigation links, service offerings, and contact information. The footer is styled to match the overall design of the website and provides essential links and information for users.

#### Dependencies

- `react-icons/fa`: For importing Font Awesome icons used in the footer.

#### Functions

| Function | Parameters | Returns | Description |
|----------|------------|---------|-------------|
| Footer | None | JSX Element | Renders the footer component with brand information, navigation links, services, and contact details. |

#### Configuration / Constants

- `currentYear`: The current year is dynamically calculated and displayed in the footer.

#### Notes / Edge Cases

- The `onMouseEnter` and `onMouseLeave` event handlers are used to change the color of the navigation and contact links on hover for better user experience.
- The `target="_blank" rel="noopener noreferrer"` attributes are used in the LinkedIn link to open the link in a new tab and ensure security.
- The `animation: 'breathe 2s ease-in-out infinite'` CSS property is used to create a breathing effect for the status badge, indicating availability.

### src/sections/Hero.jsx

#### Overview

# Hero.jsx Documentation

#### Module Overview

The `Hero.jsx` file is responsible for rendering the hero section of the application. It includes a spotlight logo effect, an interactive dot grid canvas, and various animated elements using GSAP. The purpose of this file is to create an engaging and visually appealing introduction to the application.

#### Dependencies

- `useEffect`, `useRef` from 'react'
- `gsap` from 'gsap'
- `ScrollTrigger` from 'gsap/ScrollTrigger'

#### Functions

| Function          | Parameters | Returns | Description                                                                                     |
| ----------------- | ---------- | ------- | ----------------------------------------------------------------------------------------------- |
| `SpotlightLogo`   | None       | JSX     | Renders the spotlight logo with a cinematic cursor-following reveal effect.                     |
| `HeroCanvas`      | None       | JSX     | Renders an interactive dot grid canvas that reacts to mouse movements.                          |
| `Hero`            | None       | JSX     | Renders the hero section, including animations for loader, badge, heading, subtitle, and CTA.    |

#### Configuration / Constants

- `LERP_SPEED`: 0.16 (for snappier tracking in `SpotlightLogo`)
- `SPOTLIGHT_RADIUS`: 280 px (soft circle radius in `SpotlightLogo`)
- `GLOW_SIZE`: 350 px (outer glow ring size in `SpotlightLogo`)

#### Notes / Edge Cases

- The `SpotlightLogo` component uses `useRef` for state management to avoid re-renders at 60fps.
- The `HeroCanvas` component uses `requestAnimationFrame` for smooth animations and reacts to window resize events.
- The `Hero` component utilizes GSAP for complex animations and ensures the loader is hidden after its animation completes.

### src/sections/Philosophy.jsx

#### Overview

# src/sections/Philosophy.jsx

#### Module Overview

The `Philosophy.jsx` file defines the "Our Philosophy" section of the website. This component utilizes GSAP animations to create engaging visual effects as users scroll through the section. It emphasizes the company's approach to software engineering and system design.

#### Dependencies

| Dependency | Type | Description |
| --- | --- | --- |
| `useEffect` | React Hook | Manages side effects in functional components. |
| `useRef` | React Hook | Provides a way to reference DOM elements. |
| `gsap` | JavaScript Animation Library | Used for creating animations. |
| `ScrollTrigger` | GSAP Plugin | Adds scroll-based triggers to GSAP animations. |

#### Functions

| Function | Parameters | Returns | Description |
| --- | --- | --- | --- |
| `Philosophy` | None | JSX Element | Renders the "Our Philosophy" section with animated text and a decorative line. |

#### Notes / Edge Cases

- Ensure GSAP and its plugins are properly installed and imported.
- The animations are designed to trigger on scroll. Ensure the section is sufficiently long to trigger the animations.
- The `useEffect` hook cleans up the GSAP context to prevent memory leaks.

### src/sections/Services.jsx

#### Overview

# `src/sections/Services.jsx` Documentation

#### Module Overview

The `Services.jsx` file is a React component that renders a section showcasing the various services offered by the company. It utilizes GSAP for animations and ScrollTrigger for scroll-based interactions. The component is designed to present a visually engaging and interactive experience for users exploring the company's capabilities.

#### Dependencies

- `useEffect`, `useRef` from 'react'
- `gsap` from 'gsap'
- `ScrollTrigger` from 'gsap/ScrollTrigger'
- Various icons from 'react-icons/fa' and'react-icons/si'

#### Functions

| Function | Parameters | Returns | Description |
| --- | --- | --- | --- |
| `Services` | None | JSX Element | Main component function that renders the services section. |
| `handleMouseEnter` | `el` (DOM element) | None | Handles the mouse enter event to animate the card. |
| `handleMouseLeave` | `el` (DOM element) | None | Handles the mouse leave event to reset the card animation. |

#### Configuration / Constants

### Services Array
```javascript
const services = [
    {
        title: 'Web Development',
        desc: 'High-performance web platforms built for scale, speed, and conversion.',
        tag: 'Frontend · Backend · Full-Stack',
        icons: [FaReact, FaPython, FaNode, FaDocker, FaGit, SiVite, SiVercel],
    },
    //... other services
];
```

#### Notes / Edge Cases

- Ensure that the `services` array is correctly populated with all necessary data to avoid rendering issues.
- The GSAP animations are dependent on the presence of the DOM elements; hence, ensure that the refs are correctly assigned.
- The `handleMouseEnter` and `handleMouseLeave` functions should be optimized to prevent excessive re-renders or performance issues.

### src/sections/Stats.jsx

#### Overview

# Stats.jsx Module Overview

The `Stats.jsx` file is a React component that displays a section of statistical data in a visually appealing and animated manner. It leverages the GSAP animation library to create smooth entrance animations for each stat card and counter animations for the numerical values. This component is designed to enhance user engagement by presenting key metrics in an interactive and aesthetically pleasing way.

#### Dependencies

| Dependency | Type | Description |
|------------|------|-------------|
| `useEffect` | React Hook | Manages side effects in functional components. |
| `useRef` | React Hook | Provides a way to persist values across renders without causing re-renders. |
| `gsap` | GSAP Library | Core library for animations. |
| `ScrollTrigger` | GSAP Plugin | Plugin for creating scroll-based animations. |

#### Functions

| Function | Parameters | Returns | Description |
|----------|------------|---------|-------------|
| `Stats` | None | JSX Element | Renders the stats section with animated stat cards. |

#### Notes / Edge Cases

- Ensure that the `gsap` library and its `ScrollTrigger` plugin are properly installed and imported.
- The `numberRefs` ref is used to target each stat number for individual counter animations. Ensure that the number of elements in the `stats` array matches the number of refs.
- The animations are triggered on scroll. Ensure that the `sectionRef` is correctly set to the section element to trigger the animations.
- The component is responsive and adjusts the padding of the stat cards for smaller screens using a media query.

### src/sections/Technologies.jsx

#### Overview

# `src/sections/Technologies.jsx` Documentation

#### Module Overview

The `Technologies.jsx` file is a React component designed to display a section showcasing the technologies utilized by our team. It incorporates animations using GSAP for an engaging user experience and is structured to be responsive, adapting to different screen sizes.

#### Dependencies

- `useEffect`, `useRef` from 'react'
- `gsap` from 'gsap'
- `ScrollTrigger` from 'gsap/ScrollTrigger'
- Various icons from 'react-icons/fa' and'react-icons/si'

#### Functions

| Function      | Parameters | Returns | Description |
| ------------- | ---------- | ------- | ----------- |
| `Technologies` | None | JSX | Main component function that renders the technologies section. |
| `TechGrid` | `techs`, `ref` | JSX | Renders a grid of technology icons and names. |

#### Configuration / Constants

- `leftTechs`: Array of technology icons and names for the left grid.
- `rightTechs`: Array of technology icons and names for the right grid.

#### Notes / Edge Cases

- The component utilizes GSAP animations which are reverted on unmount to prevent memory leaks.
- The tech grids are designed to stack on mobile devices for better responsiveness.
- Ensure that the GSAP plugin `ScrollTrigger` is registered before usage to avoid runtime errors.

### src/sections/Testimonials.jsx

#### Overview

# Testimonials.jsx Documentation

#### Module Overview

The `Testimonials.jsx` file is a React component designed to display a series of testimonials in a visually appealing, animated manner. It utilizes GSAP for animations and ScrollTrigger for scroll-based interactions, creating an engaging user experience. The component is intended to showcase user feedback and enhance the credibility and appeal of the Bug Biceps program.

#### Dependencies

- `useEffect`, `useRef` from 'react'
- `gsap` from 'gsap'
- `ScrollTrigger` from 'gsap/ScrollTrigger'
- `FaQuoteLeft` from'react-icons/fa'

#### Functions

| Function      | Parameters | Returns | Description                                                  |
|---------------|------------|---------|--------------------------------------------------------------|
| `renderStars` | `count`    | JSX     | Renders star icons based on the rating count provided.      |

#### Configuration / Constants

- `testimonials`: An array of objects containing testimonial data.
- `duplicated`: A duplicated array of `testimonials` for seamless infinite scroll.

#### Notes / Edge Cases

- The testimonials array is duplicated to enable an infinite scroll effect.
- The component uses GSAP's `ScrollTrigger` to animate the heading as the user scrolls.
- The marquee animation for the testimonials is paused on hover to improve user experience.
- The `renderStars` function dynamically generates star icons based on the rating, enhancing visual feedback.

